
# Set the directory containing your source files
set(SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})

# Set the directory where your executable will be built
set(BIN_DIR ${CMAKE_CURRENT_BINARY_DIR})

# Get a list of all directories in your source directory
file(GLOB directories ${SRC_DIR}/*)

# Iterate over the list of directories
foreach(directory ${directories})
  # Check if the current directory is a directory
  if(IS_DIRECTORY ${directory})
    # Add the current directory to a list of directories
    list(APPEND dir_list ${directory})
  endif()
endforeach()
1. Creating a list of directories
# Print the list of directories
message(STATUS "Directories: ${dir_list}")

#making list of directories
file( GLOB dir_list Circle Rectangle Shape src threeDimShape twoDimShape Triangle )

# Iterate over the list of directories
foreach( directory ${dir_list} )

if( IS_DIRECTORY ${directory} )
add_subdirectory ( ${directory} )
endif()

endforeach()
1. Creating a list of binaries by chatGPT
# Set the directory containing your binary files
set(BIN_DIR ${CMAKE_CURRENT_BINARY_DIR})

# Get a list of all binary files in your binary directory
file(GLOB_RECURSE binary_files ${BIN_DIR}/*.exe ${BIN_DIR}/*.dll)

# Iterate over the list of binary files
foreach(binary_file ${binary_files})
  # Add the current binary file to a list of binaries
  list(APPEND binary_list ${binary_file})
endforeach()

# Print the list of binaries
message(STATUS "Binaries: ${binary_list}")